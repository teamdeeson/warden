{% extends 'DeesonWardenBundle::layout.html.twig' %}

{% block title %}Edit site: {{ site.getName() }}{% endblock %}

{% block page_title %}Edit site: {{ site.getName() }}{% endblock %}

{% block content %}

    <p>Core Version: {{ site.getCoreVersion() }} ({{ site.getLatestCoreVersion() }}, {% if site.compareCoreVersion() %}Y{% else %}N{% endif %})</p>

    {{ form(form) }}

    <br />
    <br />

    <a href="{{ path('sites_show', {'id': site.getId() }) }}"><< Back</a>
    {#<br />
    <br />
    <a href="{{ path('sites_delete', {'id': site.getId() }) }}">DELETE</a>#}

{% endblock %}