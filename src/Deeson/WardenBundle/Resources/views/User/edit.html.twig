{% extends 'DeesonWardenBundle::layout.html.twig' %}

{% block title %}Edit user: {{ user.getUsername() }}{% endblock %}

{% block page_title %}Edit user: {{ user.getUsername() }}{% endblock %}

{% block content %}

    <div class="box box-danger">
        {{ form(form) }}
    </div>

    <a href="{{ path('admin_user_change_password', {'id': user.getId() }) }}">Change Password</a>
    <br />
    <a href="{{ path('admin_user_delete', {'id': user.getId() }) }}">Delete</a>
    <br />
    <br />
    <a href="{{ path('admin_user_list') }}">Cancel</a>

{% endblock %}
