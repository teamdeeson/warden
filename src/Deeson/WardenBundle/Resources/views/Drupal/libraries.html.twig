<div class="box">
    <div class="box-header">
        <h3 class="box-title">Installed {{ name|capitalize }} Libraries</h3>
    </div>
    <!-- /.box-header -->
    <div class="box-body table-responsive no-padding">
        <table class="table table-hover">
            <thead>
            <tr>
                <th width="60%">Name</th>
                <th>Version</th>
            </tr>
            </thead>
            <tbody>
            {% for name, version in data %}
                <tr>
                    <td>{{ name }}</td>
                    <td>{{ version }}</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
